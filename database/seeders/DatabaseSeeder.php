<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * Note: This application no longer requires database seeding.
     * Games are now configured in config/games.php.
     *
     * @return void
     */
    public function run()
    {
        // No seeders to call - games are now configured in config/games.php
    }
}
